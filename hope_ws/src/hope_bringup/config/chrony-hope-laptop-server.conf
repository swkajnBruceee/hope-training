# Keep the venue Laptop's NTP client access across chrony/HDU restarts without
# modifying the manager-approved /etc/chrony/chrony.conf package manifest.
# chrony.service runs as _chrony, which owns the local chronyd command socket.
[Service]
ExecStartPost=/usr/bin/chronyc allow PLACEHOLDER_LAPTOP_WIFI_IP/32
