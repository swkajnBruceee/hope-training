# bringup

HOPE bringup: launch files, world frame config, mocap relay.

Python launch files are kept (ROS 2 launch is normally Python); all
business logic lives in C++ packages (solver etc.) that the launch
files invoke.

