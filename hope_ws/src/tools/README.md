# tools

HOPE C++ package: see package.xml and CMakeLists.txt.

All runtime code is C++ (ament_cmake). Headers are flat under `include/`
(no nested package-name subdirectories); sources are under `src/`.

