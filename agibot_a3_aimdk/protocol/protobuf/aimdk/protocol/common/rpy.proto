syntax = "proto3";
package aimdk.protocol;

/**
 * @brief RPY角度姿态表示
 */
message Rpy {
  double rx = 1;  // 绕固定轴X旋转角度，单位：rad
  double ry = 2;  // 绕固定轴Y旋转角度，单位：rad
  double rz = 3;  // 绕固定轴Z旋转角度，单位：rad
}