请在现有 `hope-training` 项目中适配用户提供的 A3 正手/反手离线击球轨迹生成器。

待适配代码包：

```text
a3_ik_point_offline_wrapper_v2.zip
```

该代码复用第三方 `mc` 工程中的：

```text
IkPointArmSource
RobotFK
```

实现：

```text
完整 canonical strike goal
→ 正手或反手 IK
→ 100 Hz 右臂 q/dq 参考轨迹
→ CSV
→ 候选 NPZ
```

本任务的目标不是仅让代码“能够编译”，而是将它可靠地接入 `hope-training` 当前的数据、坐标、关节顺序、FK、PhysX 和 motion manifest 合同。

必须明确：

```text
生成器成功
=
KINEMATIC_CANDIDATE
```

不等于：

```text
OFFLINE_FK_QUALIFIED
FIXED_BASE_QUALIFIED
FLOATING_BASE_QUALIFIED
TRAINING_REFERENCE_APPROVED
```

任何生成结果都必须经过后续 gate，不能因为 IK 成功就直接加入正式训练。

# 一、输入代码结构

解压后预计包含：

```text
a3_ik_point_offline_wrapper_v2/
├── CMakeLists.txt
├── README.md
├── CHANGES_v2.md
├── src/
│   └── generate_strike_reference.cpp
├── scripts/
│   ├── build_and_run.sh
│   ├── generate_dual_stroke_dataset.py
│   └── csv_to_npz.py
└── examples/
    ├── goal_forehand.yaml
    ├── goal_backhand.yaml
    ├── ready_forehand_template.yaml
    ├── ready_backhand_phase0.yaml
    └── dual_stroke_manifest.json
```

请先完整阅读这些文件，不要未经审计直接复制或执行。

同时定位本地 `mc` 源码目录，确认至少存在：

```text
mc/arm/include/ik_point_arm_source.hpp
mc/arm/src/ik_point_arm_source.cpp
mc/include/control_source.hpp
mc/include/fk.hpp
mc/src/fk.cpp
mc/include/math_utils.hpp
mc/include/policy_parameters.hpp
mc/include/xml.h
mc/include/robot_io/robot_io_backend.hpp
mc/arm/hit_ik_point.yaml
mc/models/hit/kinematics/a3_t2d5.xml
```

# 二、建议的项目目录

将 wrapper 放入隔离目录，例如：

```text
hope-training/
└── tools/
    └── a3_ik_point_reference_generator/
```

第三方 `mc` 源码不要无差别复制进 `hope-training` 主代码。

推荐：

```text
hope-training/
├── third_party/
│   └── mc_match_serve_hit_v1/
├── tools/
│   └── a3_ik_point_reference_generator/
└── data/
    └── generated_strike_references/
```

如果本地已有统一的 third-party 路径，遵循现有规范。

不得覆盖现有：

```text
motion loader
CSV → NPZ builder
tracking manifest builder
PhysX evaluator
FK implementation
```

# 三、首先完成源码接口审计

在修改前生成报告：

```text
hope_training/whole_body_tracking/docs/eval_reports/
ik_point_wrapper_local_adaptation_audit.md
```

报告必须确认：

1. `IkPointArmSource` 实际命名空间；
2. 构造函数签名；
3. `Load()` 参数和返回值；
4. `SetGoal()` 参数和返回值；
5. `Update()` 参数和返回值；
6. `ArmGoal` 实际字段；
7. `ArmTarget.q/dq` 的实际维度和排列；
8. 右臂是否确实位于 `target.q[7:14]`；
9. `RobotState.q/dq` 是否确实使用 31 维 SDK 顺序；
10. `EvaluateRacketPose()` 接收的关节顺序；
11. `SwingName()`、`PhaseName()` 等诊断接口是否存在；
12. 本地源码是否与 wrapper 编写时的版本一致；
13. yaml-cpp、Eigen 和 C++ 标准要求；
14. 是否存在许可证文件，若没有则标记授权状态未确认。

若本地接口与 wrapper 不一致，应适配 wrapper，不能通过修改第三方核心算法来“凑接口”。

# 四、构建系统适配

当前 wrapper 使用：

```text
C++20
Eigen
yaml-cpp
```

并通过：

```bash
-DMC_ROOT=/absolute/path/to/mc
```

指定第三方源码。

请适配到本地项目的构建习惯，但保留独立可执行文件：

```text
a3_generate_strike_reference
```

必须支持独立构建，不依赖：

```text
ROS
AimRT
A3 HAL
真机控制进程
官方 MC/PM
ONNX Runtime
```

编译目标只能链接轨迹生成所需的最小源文件。

当前候选源文件：

```text
src/generate_strike_reference.cpp
mc/arm/src/ik_point_arm_source.cpp
mc/src/fk.cpp
```

若出现链接缺失，应根据实际符号增加最少源文件，不得直接把整个真机工程链接进来。

构建完成后保存：

```text
compiler version
CMake command
linked source list
linked libraries
binary hash
```

# 五、严禁执行真机脚本

不得运行类似：

```text
run_model3396_real.sh
停止官方MC/PM
启动HAL
向真实机器人下发关节命令
```

本任务只允许：

```text
离线规划
离线文件生成
Isaac/PhysX 仿真
```

如果发现代码路径可能触发真机控制，必须 fail-fast。

# 六、正手和反手必须显式指定

正式训练数据禁止使用 `auto`。

每个 goal 必须显式写：

```yaml
swing_type: forehand
```

或：

```yaml
swing_type: backhand
```

正式数据链：

```text
forehand goal
→ forehand READY
→ 强制 forehand IK
→ forehand 输出目录

backhand goal
→ backhand READY
→ 强制 backhand IK
→ backhand 输出目录
```

`auto` 只允许用于单独的诊断实验，不得进入正式批量数据 manifest。

原因：

```text
auto分支可能受READY姿态、IK seed和关节距离偏置
导致正反手标签污染
```

批量脚本应继续拒绝 `auto`。

# 七、Goal 输入合同

当前 goal YAML 示例：

```yaml
schema_version: a3_canonical_strike_goal/v1
goal_id: forehand_0001
frame: initial_base_heading
swing_type: forehand
position_m: [x, y, z]
linear_velocity_mps: [vx, vy, vz]
racket_normal: [nx, ny, nz]
time_to_strike_s: 1.20
sequence: 1001
```

反手同理。

必须校验：

```text
goal_id非空且唯一
position为3维finite
velocity为3维finite
normal为3维finite且非零
normal显式归一化
time_to_strike > 0
swing_type为forehand/backhand
frame合法
sequence唯一或可追踪
```

不能只给一个位置后静默继承默认：

```text
normal
velocity
time_to_strike
```

如后续需要支持“只给位置”，必须由调用者显式选择：

```text
normal_template_id
velocity_template_id
time_template_id
```

并将模板写入输出元数据。

# 八、坐标系适配是强制 Gate

当前 wrapper 接受：

```text
a3_base_yaw
initial_base_heading
```

但现有 C++ wrapper 只是接受这两个字符串，并不保证已经真正执行二者间坐标变换。

必须检查：

```text
LoadGoal()
ToArmGoal()
```

确认目标被如何传给 `IkPointArmSource`。

如果 `IkPointArmSource` 要求：

```text
a3_base_yaw
```

而输入使用：

```text
initial_base_heading
```

必须实现显式坐标转换：

```text
canonical goal in IBH
→ planner goal in a3_base_yaw
```

需要转换：

```text
position
linear velocity
racket normal
```

不得只转换位置。

必须记录：

```text
input frame
planner frame
rotation matrix
translation
input position
transformed position
input normal
transformed normal
input velocity
transformed velocity
round-trip error
```

禁止根据实时 pelvis 每帧重算目标。

Canonical goal 必须继续固定在：

```text
world
或 immutable initial-base-heading
```

微步或 root motion 不能改变 canonical target。

若本地初始化时：

```text
IBH == a3_base_yaw
```

也必须通过数值测试证明，并在配置中明确声明，不能默认假设。

# 九、31 维关节顺序必须对齐

当前 wrapper 内硬编码了 31 维 SDK 顺序，并假设右臂索引：

```text
12, 13, 14, 15, 16, 17, 18
```

必须与以下三套顺序分别比对：

```text
mc SDK joint order
READY YAML q_sdk_31 order
Isaac runtime joint_names order
```

新增显式映射文件：

```text
hope_training/whole_body_tracking/contracts/
a3_ik_point_joint_mapping_v1.json
```

格式建议：

```json
{
  "sdk_joint_names": [],
  "generator_right_arm_names": [],
  "generator_right_arm_sdk_indices": [],
  "isaac_joint_names": [],
  "sdk_to_isaac": {},
  "generator_to_isaac": {}
}
```

必须检查：

```text
31个SDK关节名称唯一
7个右臂关节存在且唯一
腰部3关节存在
没有左右臂混淆
没有同名重复
没有通过数组位置盲猜
```

如果本地 A3 关节名称与 wrapper 不同，应使用映射，不要静默改名。

# 十、核实 ArmTarget 布局

当前 wrapper 使用：

```cpp
sample.arm_q[i] = target.q[7 + i];
sample.arm_dq[i] = target.dq[7 + i];
```

这假设：

```text
ArmTarget.q = [left_arm_7, right_arm_7]
```

必须从本地 `control_source.hpp` 和 `IkPointArmSource` 写入逻辑验证。

增加单元测试：

```text
给右臂某个关节施加唯一非零测试值
确认输出落在正确索引
确认左臂输出没有被误取
```

若布局不是 `[left7, right7]`，修正 wrapper。

不得在没有证据时继续使用 `7 + i`。

# 十一、READY 状态适配

当前附带：

```text
ready_backhand_phase0.yaml
ready_forehand_template.yaml
```

它们只能作为模板。

特别是：

```text
ready_forehand_template.yaml
```

是来源代码中的正手 IK seed，不是 PhysX-qualified READY。

正式生成前，必须分别从本地环境导出：

```text
settled_forehand_ready.yaml
settled_backhand_ready.yaml
```

导出流程：

```text
加载对应READY姿态
→ reset
→ PhysX settling
→ 确认无跌倒、无明显持续滑移
→ 读取实际31维q/dq
→ 保存READY
```

每个 READY 必须包含：

```yaml
schema_version: a3_ready_state/v2
ready_id: unique_id
swing_type: forehand  # 或 backhand
verification_status: local_post_settle
q_sdk_31: [...]
dq_sdk_31: [...]
```

不要仅保存 Isaac order。

如读取的是 Isaac order，应先通过正式映射转换成 SDK order，再写入 `q_sdk_31`。

必须在输出 diagnostics 中记录：

```text
ready_id
ready_swing_type
READY文件hash
q/dq hash
```

正手 goal 使用反手 READY，或反手 goal 使用正手 READY时，必须 fail-fast。

`--allow-ready-mismatch` 只允许诊断，不得用于正式数据生成。

# 十二、反手和正手 READY 不必强制共用下肢姿态

第一版可共用相同屈膝下肢站姿，但上肢 READY 应分别定义。

若 PhysX 显示两种挥拍对重心要求不同，可以分别调整：

```text
腰部
髋roll
膝关节
踝关节
双脚间距
```

但必须保持：

```text
canonical target frame不变
正反手READY来源明确
```

任何 READY 调整都应独立通过静态 P0。

# 十三、生成器只输出候选参考

当前 wrapper 在每个时间步执行：

```text
planner.Update()
```

并将返回的右臂 q/dq 反馈为下一步 `RobotState`，即：

```text
perfect-tracking rollout
```

这只是让 planner 确定性地产生开环轨迹。

必须在代码注释、元数据和报告中明确：

```text
perfect tracking simulation
≠ actuator tracking
≠ PhysX replay
```

输出状态只能是：

```text
KINEMATIC_CANDIDATE
PLANNING_REJECTED
GENERATION_TIMEOUT
SET_GOAL_REJECTED
```

不能在生成器阶段写：

```text
qualified
teacher
physx_pass
```

# 十四、控制频率适配

当前默认：

```text
100 Hz
dt = 0.01 s
```

本地允许通过：

```text
--control-hz
```

设置，但输出文件名当前固定为：

```text
trajectory_100hz.csv
```

请改为频率感知命名：

```text
trajectory_100hz.csv
trajectory_50hz.csv
```

或者：

```text
trajectory.csv
```

并由元数据记录实际频率。

不能出现：

```text
control_hz=50
但文件仍叫trajectory_100hz.csv
```

第一版正式生成建议保持原生 100 Hz。

必须记录：

```text
control_hz
control_dt
sample_count
requested strike time
planned strike time
hit frame
trajectory duration
```

# 十五、Hit frame 合同

当前 wrapper 使用：

```text
距离 planner.PlannedStrikeTimeS() 最近的采样帧
```

作为 hit frame。

必须验证：

```text
abs(hit_frame * dt - planned_strike_time) <= dt / 2 + tolerance
```

规划器可能自动延长击球时间，因此：

```text
requested time
```

与：

```text
planned time
```

必须同时保存。

训练数据的正式 hit frame 必须使用：

```text
planned_strike_time
```

不能使用原始请求时间。

若规划器未真正完成 strike，不得仍然标一个 hit frame。

当前代码即使 generation 失败，也可能对已有 samples 选出最近帧；请确保：

```text
success=false
→ 不导出可用hit frame
或明确hit_frame_valid=false
```

# 十六、输出 CSV 适配

候选 CSV 至少保留：

```text
time_s
phase
is_hit_frame

waist_yaw_joint
waist_roll_joint
waist_pitch_joint

right_shoulder_pitch_joint
right_shoulder_roll_joint
right_shoulder_yaw_joint
right_elbow_joint
right_wrist_roll_joint
right_wrist_pitch_joint
right_wrist_yaw_joint

对应10个joint velocity

generator racket position xyz
generator racket normal xyz
```

腰部当前固定为 READY 值，必须在元数据写：

```text
waist_source = fixed_ready
```

不得暗示腰部经过优化。

建议额外输出：

```text
requested_swing_type
selected_swing_type
ready_id
goal_id
control_dt
```

如不放进每行 CSV，也必须保存在同目录 metadata 中。

# 十七、当前 csv_to_npz.py 不是正式项目格式

附带的：

```text
scripts/csv_to_npz.py
```

只生成简化的 10 DOF upper-body NPZ，并明确没有构造：

```text
Isaac body states
完整motion metadata
正式tracking manifest字段
```

因此只能用于：

```text
快速检查
中间格式
candidate artifact
```

适配时优先复用现有正式工具：

```text
data/analysis/mocap_cleaning/
cli_build_a3_csv_to_npz_commands.py
```

以及对应：

```text
csv_to_npz.py
```

请先审计正式 CSV 输入列、关节顺序、频率、root/body 数据和 target metadata 要求。

不要建立第二套长期并存、含义不一致的 NPZ 格式。

推荐：

```text
wrapper CSV
→ 项目标准化适配层
→ 官方CSV格式
→ 现有CSV→NPZ builder
```

如果必须保留简化 NPZ，命名为：

```text
candidate_upper_reference.npz
```

并标记：

```text
not_motion_library_ready = true
```

# 十八、生成器 FK 与 Isaac FK 几何 Gate

这是批量生成前最重要的门槛。

新增工具：

```text
hope_training/whole_body_tracking/scripts/
eval_ik_point_generator_fk_contract.py
```

选择至少：

```text
20组
最好100组
```

安全右臂关节状态。

分别计算：

```text
IkPointArmSource/RobotFK racket position
IkPointArmSource/RobotFK racket normal

Isaac正式FK racket position
Isaac正式FK racket normal
```

输出：

```text
per-sample position error
per-sample normal error
mean
max
P95
```

初始门槛建议：

```text
mean position error < 2 mm
max position error < 5 mm
mean normal error < 0.5°
max normal error < 1°
```

如果失败，依次排查：

```text
关节顺序
关节符号
关节零位
肩部base transform
腕部transform
球拍mount offset
球拍拍面normal axis
root heading
坐标frame
```

严禁通过统一加一个平移 offset 来掩盖链路错误。

该 Gate 未通过时：

```text
禁止批量生成
禁止PhysX资格判断
禁止加入manifest
```

# 十九、单点正手验证

选择一个已有、历史上可信的正手 reference，提取其：

```text
position
normal
velocity
time_to_hit
```

生成：

```text
forehand_single_known_goal
```

使用本地：

```text
settled_forehand_ready.yaml
```

执行：

```text
goal validation
→ planner generation
→ CSV
→ offline FK
→ fixed-base PhysX
```

输出目录：

```text
eval_outputs/ik_point_adapter/
forehand_single_known_goal/
```

至少保存：

```text
input_goal.yaml
input_ready.yaml
normalized_goal.json
trajectory.csv
diagnostics.json
offline_fk_eval.json
fixed_base_physx_eval.json
stdout/stderr
hashes.json
```

# 二十、单点反手验证

同样选择一个已有可信反手 reference：

```text
backhand_single_known_goal
```

使用：

```text
settled_backhand_ready.yaml
```

执行完全相同的 gate。

不得只验证其中一种动作后就批量生成两种数据。

# 二十一、规划器内部验收

每条候选至少记录：

```text
requested_swing_type
selected_swing_type
ready_id
solve iterations
solved position error
solved normal error
velocity solve error
achieved strike velocity
requested strike time
planned strike time
timing extension
minimum body clearance
trajectory reject reason
sample count
hit frame
```

对于显式 forehand/backhand goal，应检查：

```text
selected_swing_type == requested_swing_type
```

如果显式请求正手，但 planner 报告选择反手，应 fail-fast，不得仅写入诊断后继续保存为正手数据。

# 二十二、离线正式 FK Gate

使用 `hope-training` 的正式 FK，而不是生成器内部 FK，重新计算：

```text
TCP position
TCP normal
TCP velocity
```

在实际 hit frame 比较 canonical goal。

输出：

```text
canonical_to_reference_position_error
canonical_to_reference_normal_error
canonical_to_reference_velocity_error
timing_error
```

并逐帧检查：

```text
joint limit
joint velocity
joint acceleration
trajectory continuity
NaN/Inf
```

通过后状态升级为：

```text
OFFLINE_FK_QUALIFIED
```

若生成器内部误差很小但正式 FK 误差大，标记：

```text
GEOMETRY_CONTRACT_FAILED
```

不能继续 PhysX。

# 二十三、Fixed-base PhysX Gate

对正手和反手分别进行 fixed-base 回放。

使用相同：

```text
initial READY
actuator contract
control frequency
canonical target
seed
```

建议与原有 reference 做 paired 对照：

```text
A：已有原reference
B：生成器candidate
```

输出误差分解：

```text
canonical → generated reference
generated reference → command
command → PhysX actual
canonical → PhysX actual
```

同时记录：

```text
joint tracking error
joint phase lag
target clipping
action clipping
torque saturation
position error
normal error
velocity error
timing error
termination reason
```

状态只有在正式门槛通过后才能升级为：

```text
FIXED_BASE_QUALIFIED
```

# 二十四、Floating-base 暂时独立

当前下肢 P0 静态锁定仍需单独完成。

因此：

```text
生成器适配
fixed-base动作验证
```

可以继续。

但：

```text
FLOATING_BASE_QUALIFIED
TRAINING_REFERENCE_APPROVED
```

必须等待下肢 P0 和真实足端执行 Gate 通过。

不能因为 floating-base 站不住就否决生成器的运动学能力，也不能因为 fixed-base 命中就宣称全身合格。

# 二十五、双分支批量生成

仅当：

```text
正手单点通过geometry + offline FK + fixed-base
反手单点通过geometry + offline FK + fixed-base
```

后，才启用：

```text
generate_dual_stroke_dataset.py
```

Manifest 中每条必须显式指定：

```json
{
  "goal_path": "goals/forehand_001.yaml",
  "swing_type": "forehand"
}
```

脚本必须核对：

```text
manifest swing_type
==
goal YAML swing_type
```

不得接受 `auto`。

输出分目录：

```text
output/
├── forehand/
└── backhand/
```

并分别统计：

```text
forehand success/reject count
backhand success/reject count
position error distribution
normal error distribution
timing extension distribution
reject reason histogram
```

禁止只输出总成功率。

# 二十六、批量生成前先粗网格

不要直接做 1 cm 全空间。

推荐顺序：

```text
已知样本单点
→ 5 cm粗网格
→ 可达域地图
→ 2 cm主体
→ 边界和高变化区域1 cm加密
```

正手和反手使用各自的工作空间配置。

新增：

```text
cfg/data/
forehand_ik_point_workspace_v1.yaml
backhand_ik_point_workspace_v1.yaml
```

工作空间边界必须来自：

```text
源规划器配置
已有真实动作分布
机器人可达性
球台约束
自碰撞约束
```

不能随意定义巨大立方体。

# 二十七、同一个点可以生成两个候选

在正反手工作空间重叠区域，可以分别执行：

```text
same canonical goal
→ explicit forehand
→ forehand candidate

same canonical goal
→ explicit backhand
→ backhand candidate
```

不得用一个 `auto` 运行替代这两个显式实验。

两个候选均通过时可都保留，但必须有不同：

```text
goal_id
stroke label
READY provenance
trajectory hash
```

# 二十八、随机目标生成规则

后续允许随机采样，但每个目标必须包含完整 10D canonical goal：

```text
position
velocity
normal
time_to_strike
```

不得只随机位置而静默使用含义不明的默认速度和法向。

如果使用模板，必须记录：

```text
position_sampler_version
normal_template_id
velocity_template_id
time_template_id
random seed
workspace config hash
```

随机生成必须可复现。

# 二十九、状态分级

每个动作必须只有一个明确状态：

```text
INPUT_REJECTED
SET_GOAL_REJECTED
PLANNING_REJECTED
GENERATION_TIMEOUT
KINEMATIC_CANDIDATE
GEOMETRY_CONTRACT_FAILED
OFFLINE_FK_REJECTED
OFFLINE_FK_QUALIFIED
FIXED_BASE_REJECTED
FIXED_BASE_QUALIFIED
FLOATING_BASE_PENDING
FLOATING_BASE_REJECTED
FLOATING_BASE_QUALIFIED
TRAINING_REFERENCE_APPROVED
```

不得跳级。

特别是：

```text
KINEMATIC_CANDIDATE
≠ 合格动作
```

# 三十、测试要求

必须增加自动测试。

## 构建测试

```text
独立CMake构建成功
无ROS/HAL依赖
```

## Goal 测试

```text
非法frame拒绝
非法swing_type拒绝
零normal拒绝
NaN拒绝
负time拒绝
```

## READY 测试

```text
31维长度正确
正手goal+反手READY拒绝
反手goal+正手READY拒绝
```

## ArmTarget 索引测试

```text
正确提取右臂7维
没有提取左臂
```

## 坐标测试

```text
IBH→base_yaw变换正确
position/normal/velocity同时转换
roundtrip正确
180°heading正确
```

## Hit-frame 测试

```text
planned time与hit frame一致
失败轨迹不产生有效hit标记
```

## 确定性测试

相同：

```text
goal
READY
planner config
robot XML
control_hz
```

运行两次，要求：

```text
trajectory hash相同
diagnostics关键值相同
```

## 标签测试

```text
显式forehand最终selected=forehand
显式backhand最终selected=backhand
```

# 三十一、日志与溯源

每条输出目录必须包含：

```text
input_goal.yaml
input_ready.yaml
normalized_goal.json
diagnostics.json
trajectory.csv
candidate NPZ或正式NPZ
generator_stdout.txt
generator_stderr.txt
hashes.json
qualification.json
```

`hashes.json` 至少包括：

```text
wrapper source hash
mc source hash
planner config hash
robot XML hash
goal hash
READY hash
trajectory hash
binary hash
```

# 三十二、正式报告

提交：

```text
hope_training/whole_body_tracking/docs/eval_reports/
ik_point_dual_stroke_adapter_report.md
```

报告必须包含：

1. wrapper 放置位置；
2. 使用的 `mc` 版本；
3. 编译命令；
4. 修改过的接口；
5. 是否保留原 IK 数学；
6. 31 维关节映射；
7. ArmTarget 实际布局；
8. 正手 READY 来源；
9. 反手 READY 来源；
10. 坐标转换；
11. 球拍几何合同；
12. 正手单点结果；
13. 反手单点结果；
14. 离线 FK 结果；
15. fixed-base PhysX 结果；
16. 失败原因；
17. 当前允许的最高资格状态；
18. 是否允许批量生成；
19. 是否允许加入 candidate manifest；
20. 是否允许加入正式训练 manifest；
21. floating-base 仍待解决的问题。

# 三十三、必须提交的代码和配置

至少提交：

```text
tools/a3_ik_point_reference_generator/
```

以及：

```text
contracts/a3_ik_point_joint_mapping_v1.json
contracts/a3_forehand_ready_state_v1.yaml
contracts/a3_backhand_ready_state_v1.yaml

scripts/eval_ik_point_generator_fk_contract.py
scripts/eval_generated_reference_offline_fk.py
scripts/eval_generated_reference_fixed_base_physx.py

cfg/data/forehand_ik_point_workspace_v1.yaml
cfg/data/backhand_ik_point_workspace_v1.yaml
```

如果使用项目正式 CSV→NPZ builder，需要提交适配层和对应测试。

# 三十四、严格执行顺序

```text
1. 解压wrapper并阅读全部文件
2. 审计本地mc接口
3. 审计许可证状态
4. 完成独立编译
5. 验证ArmTarget右臂索引
6. 建立SDK→Isaac关节映射
7. 导出本地settled正手READY
8. 导出本地settled反手READY
9. 实现并测试坐标转换
10. 完成generator FK ↔ Isaac FK Gate
11. 用已知正手目标生成单点candidate
12. 完成正手offline FK
13. 完成正手fixed-base PhysX
14. 用已知反手目标生成单点candidate
15. 完成反手offline FK
16. 完成反手fixed-base PhysX
17. 适配项目正式CSV→NPZ链
18. 正反手单点全部通过后启用批量脚本
19. 先运行5 cm粗网格
20. 输出正手和反手独立统计
21. 只将FIXED_BASE_QUALIFIED加入候选manifest
22. P0通过后再做floating-base
23. 最终通过后才加入正式训练manifest
```

# 三十五、禁止事项

禁止：

- 使用 `auto` 生成正式训练数据；
- 正手和反手共用一个未经验证的 READY；
- 忽略 `selected_swing_type`；
- 直接假设 `target.q[7:14]` 是右臂；
- 只凭数组位置猜关节顺序；
- 把 `initial_base_heading` 标签当作已完成坐标转换；
- 根据实时 pelvis 移动 canonical goal；
- 使用 wrapper 简化 NPZ 冒充正式 motion NPZ；
- IK 成功后直接加入训练；
- 几何 Gate 失败后继续批量生成；
- 只验证反手却批量生成正手；
- 只验证正手却批量生成反手；
- 在失败轨迹中保留有效 hit frame；
- 将 perfect-tracking rollout 作为 PhysX 结果；
- 在 P0 未通过前宣称 floating-base qualified；
- 运行任何真机控制脚本。

# 三十六、完成标准

本任务完成的标准不是：

```text
wrapper编译成功
```

也不是：

```text
生成了正手和反手CSV
```

而是：

> `hope-training` 能够接收显式标注为正手或反手的完整 canonical strike goal，分别使用本地 PhysX settling 后验证的正手/反手 READY，通过明确的坐标转换和关节映射调用 `IkPointArmSource`，确定性地产生 100 Hz 右臂候选轨迹。生成器 FK 必须与 Isaac 正式 FK 一致，候选轨迹必须通过正式离线 FK 和 fixed-base PhysX Gate，正手和反手的数据、READY、标签、统计和输出目录必须完全隔离。只有通过相应资格门槛的动作才能进入候选 manifest；floating-base 与正式教师资格必须继续独立验证。