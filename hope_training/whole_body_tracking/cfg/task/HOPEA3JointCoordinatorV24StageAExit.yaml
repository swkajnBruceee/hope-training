# V24: retain the frozen Stage-A brace through forward-momentum arrest, then
# retire only its sagittal channels with a latched, state-conditioned exit.
defaults:
  - HOPEA3JointCoordinatorV23CaptureRecovery
  - _self_

name: HOPEA3JointCoordinatorV24StageAExit
experiment_name: agibot_a3_joint_coordinator_v24_stage_a_exit_20260728

actions:
  stage_a_sagittal_exit_enabled: true
  stage_a_sagittal_exit_center_half_width_m: 0.04
  stage_a_sagittal_exit_velocity_deadband_mps: 0.03
  stage_a_sagittal_exit_positive_confirm_steps: 2
  stage_a_sagittal_exit_neutral_confirm_steps: 2
  stage_a_sagittal_exit_decay_steps: 5
  stage_a_sagittal_exit_require_both_feet: true
