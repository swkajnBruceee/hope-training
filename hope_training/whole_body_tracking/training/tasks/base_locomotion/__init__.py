"""A3 Base Policy tasks.

The first registered task is deliberately limited to deterministic, zero-command
Stand smoke validation.  Locomotion commands and strike intervention are gated
separately and must not be added to this configuration implicitly.
"""

