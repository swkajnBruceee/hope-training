# Archived Sample Motion Libraries

These directories are not active training inputs.

They are preserved only for reference, debugging, and comparison against older processing strategies.

Current training should use:

```text
../p2_fixed_competition_global_funnel_balanced20/manifest.json
```

The old `p2_fixed_competition*` library entry points were deleted because they were derived symlink trees and could become misleading after output archives are moved.

Their source manifests and NPZ outputs are preserved under:

```text
data/analysis/mocap_cleaning_outputs/DATA260708_post1p0/_archive_not_for_training/
```
